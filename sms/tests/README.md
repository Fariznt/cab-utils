# Test Suite

This directory contains a test suite that was generated with Opus & Sonnet 5. Implementation has received minimal manual review to streamline development and a launch, so tests should be treated as a starting point rather than a fully vetted suite. The flow was manually tested end-to-end via texting separately.

Before relying on these tests for correctness guarantees, please review them first, especially around edge cases and assertions.